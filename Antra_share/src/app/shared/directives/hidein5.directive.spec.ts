import { Hidein5Directive } from './hidein5.directive';

describe('Hidein5Directive', () => {
  it('should create an instance', () => {
    const directive = new Hidein5Directive();
    expect(directive).toBeTruthy();
  });
});
