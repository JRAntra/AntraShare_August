import { RegisterNowDirective } from './register-now.directive';

describe('RegisterNowDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterNowDirective();
    expect(directive).toBeTruthy();
  });
});
